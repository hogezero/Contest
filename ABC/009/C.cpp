#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
//#define DEBUG
using namespace std;

int main(){
    int N, K;
    vector<char> S(N);

    for(int i = 0; i < N; i++){
        cin >> S.at(i);
    }
}