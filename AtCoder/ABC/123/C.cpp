#include <iostream>
#include <vector>
#include <string>
#include <queue>
//#define DEBUG
using namespace std;

int main(){
    int N;
    vector<int> trans(5);
    vector<int> ans(6);
    cin >> N;
    for(int i = 0; i < trans.size(); i++){
        cin >> trans.at(i);
    }
    queue<int> q;
    q.push(N);

    while(!q.empty()){
        int now = q.front();
    }

    int counter = 0;


}