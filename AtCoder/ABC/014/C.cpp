#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <iomanip>


//#define DEBUG
using namespace std;

// 累積和
int main(){
    int n;
    cin >> n;
    vector <int> a(n);
    vector <int> b(n);
    int ans = 0;
    for(int i = 0; i < n; i++){
        cin >> a.at(i) >> b.at(i);
    }

}